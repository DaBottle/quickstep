#include "log/LogRecord.hpp"
#include "log/LogManager.hpp"

namespace quickstep {

  int main(int argc, char** argv) {
    LogManager log_manager = new LogManager();
    LogRecord log_record = new LogRecord(1, 1);
    
  }

}
